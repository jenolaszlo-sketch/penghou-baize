# Quick start sample

This sample demonstrates provider registration, fluent routing, a named route, and a no-inference routing explanation.

```powershell
dotnet run --project samples/Penghou.Baize.QuickStart
```

Ollama does not need to be running for the explanation because no request or startup endpoint validation is performed.
